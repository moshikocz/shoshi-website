# רשימת בדיקות לאתר איכותי — לפני שסוגרים משימה

מסמך עקרונות, לא מסמך build. המטרה: כל סוכן AI שבונה/עורך אתר יעבור על הרשימה
הזו ויידע **מה לבדוק ולמה**, בלי תלות בפרויקט ספציפי. נכתב מתוך לקחים אמיתיים
מפרויקט אתר שושי מיראז — הדוגמאות מסומנות ב-`(דוגמה)`, אבל העקרונות כלליים
ונועדו להיות ניידים לכל פרויקט אתר סטטי/build-in-place.

כל סעיף כולל: **מה לבדוק**, **למה זה חשוב**, **איך מאמתים בפועל**.

---

## 1. נתונים מובנים ונראות לסוכני AI (לא רק Google הקלאסי)

### 1.1 JSON-LD (Schema.org)
**מה:** כל אתר עסקי צריך `LocalBusiness`/`ProfessionalService` (שם, כתובת, קטגוריה,
sameAs לרשתות חברתיות) על כל עמוד, ועוד סכימה ספציפית לפי סוג התוכן —
`Service`+`Offer`, `BlogPosting`, `Product`, `Course` וכו'.
**למה:** בלי זה, מנועי AI (ChatGPT/Perplexity/Google AI Mode) ומנועי חיפוש
קלאסיים לא יכולים "להבין" את המבנה של הדף — רק לנחש מטקסט חופשי.
**איך מאמתים:** להריץ [Rich Results Test](https://search.google.com/test/rich-results)
או Lighthouse (SEO → "Structured data is valid" — לא מאומת אוטומטית, בדיקה ידנית).

### 1.2 `llms.txt` — פורמט מדויק, לא רק "יש קובץ"
**מה:** קובץ Markdown בשורש עם `# כותרת`, `> תקציר`, וסעיפי `##` עם **לינקים
אמיתיים בפורמט `[שם](url): הערה`** — לא `שם — הערה: /path`.
**למה:** Lighthouse (קטגוריית "Agentic browsing", חדשה) בודק את זה באופן
פורמלי, וסוכני AI שסורקים את הקובץ מצפים לפורמט markdown-link תקני
(ר' [llmstxt.org](https://llmstxt.org)). קובץ עם פורמט לא-תקני עלול להיחשב
כלא-קיים בפועל.
**איך מאמתים:** PageSpeed Insights → Agentic browsing → "llms.txt follows
recommendations". `(דוגמה: זה בדיוק מה שתפס אותנו — היה לנו llms.txt עם
תוכן נכון אבל פורמט "title — desc: /path" שגוי; לקח 5 דקות לתקן ברגע שהבנו
מה בדיוק ה-audit בודק.)`

### 1.3 `sitemap.xml` עם `lastmod` אמיתי
**מה:** `<lastmod>` לכל URL, לא רק `<loc>`.
**למה:** בלי זה, סורקים לא יודעים מה בכלל השתנה ומה כדאי לסרוק מחדש —
מבזבז תקציב סריקה (crawl budget) על תוכן שלא זז.
**איך מאמתים:** אם ה-sitemap נכתב ידנית, הוא **יתיישן** ברגע שיש CMS/content
collection שמוסיף עמודים — הפתרון הנכון הוא ליצור אותו מתוך ה-build script
עצמו (לא קובץ סטטי נפרד), עם תאריך אמיתי (`git log` על קובץ המקור, או
תאריך frontmatter של הפוסט).

### 1.4 `robots.txt` — לחסום גם נתיבי build פנימיים
**מה:** לא רק `/admin/` — גם כל נתיב שהוא "פלט build לא-מוגמר" או "תוכן פנימי
בלבד" (ר' סעיף 3).
**למה:** מפורט למטה.

---

## 2. ביצועים — לפני שקופצים לפתרון כבד

### 2.1 מדדים אמיתיים, לא ניחוש
**מה לבדוק:** Core Web Vitals דרך [PageSpeed Insights](https://pagespeed.web.dev)
או Lighthouse — LCP (סף בעייתי: >2.5s), CLS (>0.1), TBT/INP.
**למה:** "מרגיש לאט" הוא לא מדד. תמיד למדוד לפני שמחליטים על פתרון.

### 2.2 קו-בסיס לפני שינוי, לא רק אחריו
לשמור תוצאת מדידה (ציון + מדדים מרכזיים) **לפני** שמתחילים לעבוד על
ביצועים, כדי שיהיה למה להשוות. בלי זה אי אפשר לדעת אם שינוי עזר או הזיק.

### 2.3 אל תבהלו ממדידה בודדת חריגה
נתוני Lighthouse Lab הם **סימולציה חד-פעמית**, רגישה מאוד לעומס רגעי על
המכונה שמריצה את הבדיקה. `(דוגמה: קיבלנו Performance 60 אחרי שהיה 98
בעקביות בשלוש בדיקות קודמות, עם TBT קפצה מ-0ms ל-1200ms בלי שום שינוי קוד
שמסביר את זה. הרצה חוזרת חזרה ל-96. זה היה רעש מדידה, לא רגרסיה.)`
**הכלל:** ממצא ביצועים דרמטי וחד-פעמי → להריץ שוב לפני שמתקנים משהו.
ממצא שחוזר על עצמו לאורך כמה בדיקות → אמיתי.

### 2.4 מסגרות CSS מ-CDN עולות ביצועים אמיתיים
**מה:** Tailwind/Bootstrap וכו' דרך `<script src="cdn...">` נוחים לפיתוח מהיר
אבל טוענים את **כל** הספרייה (לא רק מה שבשימוש) — render-blocking + JS לא
בשימוש.
**למה זה בסדר בהתחלה:** לאתר סטטי קטן (עשרות עמודים) זה שולי ולא מצדיק
בנדלר. **מתי זה כן בעיה:** אם LCP נשאר מעל 2.5s **אחרי** תיקונים ממוקדים
אחרים (למטה), זה הזמן לשקול build מקומפל.

### 2.5 תמונות בגודל התצוגה בפועל, לא בגודל המקור
**מה לבדוק:** "This image file is larger than it needs to be (NxM) for its
displayed dimensions (WxH)" ב-Lighthouse.
**למה:** תמונה 800×800 שמוצגת ב-144×144 מבזבזת רוחב פס בלי שום תועלת
ויזואלית. Pipeline אוטומטי (resize + WebP) בזמן ההעלאה פותר את זה לצמיתות.

### 2.6 `width`/`height` מפורשים על כל `<img>`
**מה:** גם עם `loading="lazy"`.
**למה:** בלי זה, הדפדפן לא יודע לשמור מקום לתמונה לפני שהיא נטענת → הזזת
פריסה (CLS) — גם אם ה-CLS הכולל נמדד נמוך, זה diagnostic ש-Lighthouse
תמיד יסמן.

---

## 3. חשיפת קבצים — מה שרצים סטטיים חושפים בטעות

### 3.1 `publish = "."` (Netlify/וכו') חושף את **כל** תיקיית ה-repo
**מה לבדוק:** אם אין `dist/` נפרד ותיקיית הפרסום היא שורש ה-repo, **כל קובץ
שנמצא ב-git נגיש דרך HTTP** — כולל:
- קבצי **מקור/תבנית** לא-מעובדים (לפני build) — אם יש מערכת build-in-place,
  לוודא שקבצי `src/`/templates לא נגישים כעמודים "שבורים" כפולים.
- תיקיות **תוכן פנימי בלבד** (בקשות/backlog/הערות פנימיות דרך CMS) —
  אלה לא אמורות להיות בכלל reachable מבחוץ.
**למה:** גם אם אין שם "סוד" טכני, זה תוכן כפול/שבור שנסרק בטעות (פוגע
ב-SEO), ולפעמים מידע עסקי פנימי (מחירים לא-רשמיים, תכנון) שלא אמור לצאת
לציבור.
**איך מתקנים:** קובץ `_redirects` (או מקביל) שמחזיר 404 על הנתיבים
האלה + `Disallow` תואם ב-`robots.txt` כשכבה שנייה. `(דוגמה: גילינו
ש-src/pages/*.html — 14 קבצי תבנית עם placeholder-ים לא-מעובדים — היו
נגישים ישירות ב-URL כי אין dist/ נפרד. Google/AI crawler שהיה מגיע לשם
היה רואה HTML שבור.)`

### 3.2 בדוק את זה לפני שסומכים על `.gitignore`
`.gitignore` מונע מקובץ להגיע ל-git מלכתחילה — זה **לא** אותו דבר כמו
"הקובץ לא נגיש באתר החי". לבדוק בפועל עם `git ls-files` מה נמצא ב-repo,
לא רק מה שנראה בעין בתיקייה המקומית.

---

## 4. נגישות (Accessibility) — הבאגים החוזרים

### 4.1 `aria-hidden="true"` + אלמנט פוקוסבילי בפנים = תמיד באג
**מה:** כל container עם `aria-hidden="true"` שמכיל `<button>`/`<a>`/`<input>`
בלי `tabindex="-1"` עליהם — נכשל תמיד ב-audit "Accessibility tree is not
well-formed" (וגם פוגע בגולשי מקלדת/קורא-מסך אמיתיים).
**למה זה קורה לרוב:** טריק נפוץ לקרוסלות אינסופיות — משכפלים שקופית
אמיתית (`cloneNode(true)`) ומסמנים את השכפול `aria-hidden`, אבל שוכחים
שהתוכן המשוכפל מכיל כפתור/לינק שעדיין תפוס ל-Tab.
**התיקון:** אחרי כל `cloneNode`, להריץ
`clone.querySelectorAll('button, a, input, textarea, select, [tabindex]')`
ולתת `tabindex="-1"` לכל תוצאה.
**איפה לחפש:** `grep -r "cloneNode" --include="*.html" --include="*.js" .`
— לבדוק כל תוצאה.

### 4.2 יחס ניגודיות — לחשב, לא לנחש
**מה:** WCAG AA דורש **4.5:1** לטקסט רגיל, **3:1** לטקסט גדול (≥18.66px,
או ≥14px **bold**).
**נוסחה מהירה לבדיקה ידנית:** לחשב luminance יחסי של כל צבע
(`0.2126·R + 0.7152·G + 0.0722·B` על ערכי RGB ה-"linearized"), ואז
`(L_בהיר + 0.05) / (L_כהה + 0.05)`. בפועל — פשוט יותר להשתמש בכלי כמו
[WebAIM Contrast Checker](https://webaim.org/resources/contrastchecker/).
**המקומות שהכי נשכחים:** טקסט על footer כהה (opacity נמוך על לבן),
כפתורי CTA (טקסט לבן על צבע מותג בהיר מדי), תוויות/labels קטנות בצבע
משני. `(דוגמה: כפתור CTA ראשי עם טקסט לבן על var(--olive-mid) מדד 3.71:1
— נכשל. המעבר ל-var(--olive-dark), טוקן שכבר קיים במערכת, נתן 5.9:1.)`
**עיקרון לתיקון:** להעדיף טוקן כהה יותר שכבר קיים במערכת העיצוב על פני
המצאת צבע חדש.

### 4.3 גודל אזור מגע (touch target) — מינימום ~24px
**מה:** כפתורים קטנים (נקודות פאג'ינציה, אייקונים) לרוב מתוכננים ויזואלית
קטנים בכוונה (8-9px) — אבל שטח הלחיצה בפועל צריך להיות גדול יותר.
**התיקון בלי לשנות מראה:** `width/height: 24px` (המינימום), `padding` כך
שה-content-box הפנימי יהיה בגודל הוויזואלי המקורי, ו-
`background-clip: content-box` כדי שהצבע ייצבע רק בפנים. כך הכפתור נראה
אותו דבר אבל שטח הלחיצה גדל.

### 4.4 לתקן את המקור המשותף, לא רק את העמוד שנבדק
אם ה-audit בדק רק עמוד אחד אבל הבאג הוא בקומפוננטה/סקריפט משותף
(קובץ JS/CSS משותף, או תבנית שמשוכפלת ידנית בכמה קבצי HTML) — לתקן
בכל המקומות שמשתמשים באותה תבנית, לא רק בעמוד שנבדק. אחרת הבאג יחזור
בפעם הבאה שיוסיפו עמוד מאותה תבנית.

---

## 5. תהליך אימות — לפני שסוגרים

1. **קו-בסיס לפני שינוי** (ביצועים/נגישות) — ר' 2.2.
2. **תיקון**.
3. **בדיקה ויזואלית מקומית** (screenshot על שרת local) — לוודא שהתיקון
   הטכני לא שבר משהו ויזואלי.
4. **Deploy + בדיקה על הסביבה החיה** — לא על local, כי build steps
   (אופטימיזציית תמונות, generation) יכולים להתנהג אחרת.
5. **זהירות מ-lag של deploy:** אם בודקים מיד אחרי push, ייתכן שה-deploy
   עוד לא הסתיים והבדיקה תראה מצב ישן. אם משהו נראה "עדיין שבור" מיד
   אחרי תיקון — לוודא ישירות מול הקוד החי (`curl <url> | grep <marker>`)
   לפני שמניחים שהתיקון לא עבד.
6. **הרצה חוזרת לפני מסקנה על רגרסיה** — ר' 2.3.

---

## 6. עקרון-על: תיעוד אוטומטי > תיעוד ידני

כל קובץ שנוצר/מתעדכן על סמך תוכן אחר (sitemap מתוך רשימת עמודים, llms.txt
מתוך תיאורי שירותים, README מתוך config) — **לגרום לו להיווצר אוטומטית
מתוך ה-build**, לא לתחזק אותו ידנית במקביל למקור האמת. קובץ ידני-בעיקרון
יתיישן ברגע שמישהו (או CMS) משנה את המקור ושוכח לעדכן את הקובץ הנגזר.
