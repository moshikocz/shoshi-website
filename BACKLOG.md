# Backlog — משימות לאתר

מקור האמת למעקב משימות. עדכנו כאן קודם, ואז את ה-Artifact הוויזואלי בהתאם.
תצוגה ויזואלית: https://claude.ai/code/artifact/a156aaeb-bd4c-4787-b9a8-236f62b3b87b

---

## 🔴 דחוף / קריטי
_(ריק כרגע — משימות דחופות "קופצות" לכאן ומעל הכל)_

---

## 🟡 רגיל

### ☐ הטמעת אנליטיקס — GA4 + Search Console
**נוסף:** 2026-09-05

מעקב תנועה, מקורות הגעה, זמן שהייה ודפים פופולריים.

**הוחלט:**
- **GA4 בלבד** + **Google Search Console**. לא Microsoft Clarity (nice-to-have, סקריפט נוסף לתחזק ב-16 קבצים + גילוי נאות נוסף — אפשר להוסיף בעתיד אם יתברר שחסרה הבנה של *איך* גולשים מתנהגים, לא רק *כמה*). לא Netlify Analytics (בתשלום, לא נדרש כרגע).
- האתר מתארח ב-**Netlify** (יש `_headers`, אין `netlify.toml`) — פריסה דרך git push, לא FTP/cPanel.
- **תלות חוסמת:** המשתמשת צריכה להירשם בעצמה ל-Google Analytics (יצירת נכס Web, קבלת `G-XXXXXXX`) — לא ניתן להירשם בשמה.

**מה כולל הביצוע בפועל:**
1. סניפט gtag.js ל-`<head>` של כל 16 הקבצים: `index.html`, `about.html`, `accessibility.html`, `blog.html`, `blog-sgirat-agan.html`, `friends.html`, `privacy.html`, `terms.html`, `service-halitot.html`, `service-livui-rigshi.html`, `service-maagal-nashim.html`, `service-sauna-adim.html`, `service-sgirat-agan.html`, `course-isha-magalit.html`, `chocolate-landing.html`, `design-system/landing-page-template.html`.
2. תג meta אימות בעלות ל-Google Search Console (אותו בלוק).
3. עדכון `privacy.html:244` — כרגע מצהיר שאין שימוש בעוגיות מעקב/אנליטיקס; לא נכון יותר ברגע שמטמיעים GA4.
4. תיעוד ב-`CLAUDE.md` תחת "Site-Wide Components" (כמו כפתור הוואטסאפ) שהסניפט חובה בכל דף חדש.
5. אימות: לאחר push לייב ב-Netlify, בדיקת לשונית Realtime ב-GA4.

**סטטוס:** ממתין לפתיחת חשבון GA4 על ידי המשתמשת.

---

### ☐ פאנל ניהול עצמאי לעריכת תוכן — Decap CMS
**נוסף:** 2026-09-05

הוחלט על **Decap CMS** (חינם, קוד פתוח, git-based) על פני Webflow — מתחבר ישירות לתשתית ה-`src/pages/`+`partials/`+`build-pages.mjs` הקיימת בלי לזרוק אותה, ותוכן נשאר קבצים רגילים בגיט (לא נעילה לפלטפורמה קניינית).

**מה כולל הביצוע בפועל (בוצע בקוד, ממתין להגדרות חשבון):**
1. `netlify.toml` — פקודת build אוטומטית (`optimize-images.mjs --auto` + `build-pages.mjs`) כך ש-commit של שושי מפרסם לבד.
2. `build-pages.mjs` הורחב לתמוך ב-`content/pages/*.json` (שדות טקסט/גודל/גופן/צבע לפי placeholder), `content/blog/*.md` ו-`content/services/*.md` (קולקציות markdown שמייצרות עמודים אוטומטית + מעדכנות רשימות), ורשימת קולגות ב-`friends.html` כשדה "רשימה".
3. `admin/config.yml` + `admin/index.html` — ממשק Decap מלא בעברית, ללא מונחי git, עם 4 קולקציות: עמודי האתר (כרגע רק עמוד הבית + חברות, כהוכחת מנגנון — שאר 13 העמודים ממתינים לאותה תבנית), שירותים, בלוג, ופניות לצוות (לא ציבורי בכלל).
4. `netlify/functions/auth.js` + `callback.js` — התחברות דרך GitHub OAuth (לא Netlify Identity, שהוצא משימוש לאתרים חדשים).
5. הבלוג שופץ למבנה קולקציה אמיתי — הפוסט הקיים (טקס סגירת אגן) הועבר ל-`content/blog/sgirat-agan.md`; `blog.html` מייצר את רשימת הכרטיסים אוטומטית במקום קוד קשיח.
6. `optimize-images.mjs` קיבל מצב `--auto` שסורק תמונות שהועלו ולא עברו אופטימיזציה ומטפל בהן לבד בזמן ה-build.

**תיקון אגב שנמצא בדרך:** גילינו ש-`chocolate-landing.html` ו-`course-isha-magalit.html` (עמודי נחיתה עצמאיים) עדיין נכללו בלולאת ה-build למרות שה-CLAUDE.md מציין שהם לא אמורים להיות חלק מהמערכת — הרצת build הייתה מוחקת בשקט תיקונים ידניים אחרונים על `course-isha-magalit.html` (כמו תיקון ה-padding מ-2026-09-23). תוקן ב-`build-pages.mjs` עם רשימת exclude מפורשת.

**נותר לביצוע — פעולות שדורשות גישה לחשבונות, לא ניתנות לביצוע מהסוכן:**
- רישום GitHub OAuth App (ב-github.com/settings/developers) עם callback URL `https://<domain>/.netlify/functions/callback`, והגדרת `OAUTH_CLIENT_ID`/`OAUTH_CLIENT_SECRET` כמשתני סביבה ב-Netlify.
- הוספת שושי כ-collaborator עם הרשאת כתיבה לריפו ב-GitHub (היא לא נכנסת ל-GitHub בעצמה — רק ל-`/admin`).
- בדיקת דיפלוי ראשון ב-Netlify ווידוא שה-build עובר וההתחברות עובדת בפועל (לא ניתן לבדוק OAuth אמיתי מקומית).
- אימות ש-`repo:` ב-`admin/config.yml` (`moshikocz/shoshi-website`) תואם את מה ש-GitHub App מוגדר עליו.

**שלב הבא (לא בוצע בסבב הזה):** הרחבת מנגנון ה-placeholder ל-13 העמודים הנותרים; יכולת "פתיחת דף נחיתה חדש מתבנית" מלא (כמו קורס/שוקולד) — נדחה כי אלה עמודים עצמאיים שלא מחוברים למנגנון partials.

---

## ⚪ רעיון / לעתיד

- **LocalBusiness JSON-LD** — חוסם על אימות שעות פעילות + כתובת מדויקת מהמשתמשת.
- **Service + FAQPage JSON-LD** לכל עמוד שירות — נשען על הטיוטות תוכן למטה.
- **הרחבת תוכן עמודי שירות** (ליווי רגשי הוליסטי / טקס סגירת אגן / סאונת אדים לאגן) — טיוטות מלאות כבר כתובות (תג עליון, מידע מהיר, "למי מתאים", FAQ). חוסם: מחיר+משך מפגש בודד/חבילה ל"ליווי רגשי הוליסטי" (חסר). כולל: התייחסות מפורשת לפלחי גיל (20 / 30-40 / 40-50) בכל עמוד שירות ("מתאים גם אם את... בתחילת הדרך / אחרי לידה / בגיל המעבר"), והדגשת הצעת "זוג נשים" (550₪) בסאונת אדים כמוצר נפרד ולא רק שורה במחירון.
- **דף הבית: באנר דחיפות + שורת אמון + FAQ כללי** — חוסם: תאריך/מחיר עדכניים של מחזור הקורס הבא (התאריך 17.8 שהופיע בטיוטה המקורית כבר עבר).
- **גדלי og:image קטנים מהמומלץ (1200×630)** — נבדק בפועל (05.09.2026, אומת שוב 23.09.2026) עם `node audit-site.mjs`. מקור אמת: `src/pages/*.html` + עמודי CMS שנוצרים מ-`content/blog/*.md`/`content/services/*.md` (הסקריפט עודכן ב-23.09.2026 לכסות גם את אלה — קודם לכן `blog-sgirat-agan.html` היה נופל בשקט מהבדיקה אחרי שמקור התוכן שלו עבר ל-`content/blog/sgirat-agan.md`). 6 מתוך 9 תמונות ogImage ייחודיות קטנות מדי: `shoshi_outdoor_seated.webp` (840×811, about.html), `service_group.webp` (1000×546, friends/course-isha-magalit/service-maagal-nashim), `rebozo-closing-ritual.webp` (1024×1024, blog-sgirat-agan), `livui_rigshi_main_page_service.webp` (800×533), `sgirat_agan_service_main_page.webp` (537×800 — פורטרט, יחס גובה-רוחב הפוך), `steam_sauna_service.webp` (598×800 — גם פורטרט). **חוסם:** אין תמונות מקור גדולות יותר בגיבוי (`image_originals_backup/`) לחלק, ולשאר הרזולוציה של המקור זהה לתוצר — הגדלה (upscale) תפגע בחדות ולא תבוצע בלי אישור. שתי אופציות לבחירה: (א) לצלם/לקבל תמונות חדשות ברזולוציה גבוהה יותר לשימוש ספציפי כ-og:image, או (ב) להחליף זמנית ל-fallback גנרי קיים בגודל תקין (`hero-main.webp`, 1920×1280) לעמודים הפגועים, במחיר של תצוגה מקדימה פחות ממוקדת-עמוד. `hero-main.webp` ו-`SHOSHI1.webp` ו-תמונת השוקולד כבר תקינות.
- **Tailwind build מקומפל + דחיסת תמונות** (כיום Tailwind נטען מ-CDN בכל עמוד) — עבודת תשתית נפרדת, אחרי שהתוכן מתייצב.
- **Google Business Profile + בקשת ביקורות מלקוחות קיימות** — פעולת המשתמשת, לא קוד. תוכן מוכן להעתקה: שם עסק, קטגוריה, תיאור עסקי עד 750 תווים, כל שירות כ"שירות" נפרד עם מחיר, שעות (לאמת מול המשתמשת), 3-4 שאלות Q&A לדוגמה, ובקשת ביקורות מלקוחות קיימות (יש מאגר עדויות באתר להעביר לגוגל).
- **הטמעת Meta Pixel** — עלה בייעוץ החיצוני לצד GA4, אבל **לא מופיע בהחלטה המתועדת למעלה** (רק GA4+Search Console הוחלטו במפורש, ב-05.09.2026). צריך לוודא עם המשתמשת אם ויתרה עליו במכוון או שצריך להוסיף כפריט נפרד לצד/בתוך פריט האנליטיקס הקיים.
- **llms.txt** — קובץ טקסט בשורש עם תיאור קצר של העסק/השירותים/המחירים, מיועד לסורקי AI (GPTBot/ClaudeBot/PerplexityBot וכו'). תקן ניסיוני, סיכון נמוך.
- **קישור/הזכרה ממכון ההכשרה בגינקוסופיה** — לציין את שם המכון המדויק בעמוד "אודות" ולבקש קישור/הזכרה חזרה ממנו; Google AI Overview לשאלת "גינקוסופיה" כבר מצטט את המכון הזה כמקור סמכותי בנישה.
- **כפתור "רכשי שובר מתנה" נפרד** בעמוד טקס סגירת אגן (כרגע רק אזכור טקסטואלי בתוך הפסקה, לא כפתור עצמאי) + הרחבת שוברי מתנה לכל השירותים, לא רק לטקס.
- **מסלול upsell מוגדר בין שירותים** — למשל מסיימת קורס אשה מעגלית מקבלת הצעה ממוקדת וברורה למעקב עם ליווי פרטני או טקס סגירת אגן במחיר מוזל, כרצף מובנה ולא כבונוס חד-פעמי סמוי בטקסט.
- **רשימת תפוצה למחזורי קורס** — לפנות ישירות ל"מתעניינות" שלא נרשמו למחזור הנוכחי כשנפתח מחזור הבא, בלי לחכות שהן ימצאו את זה בגוגל מחדש.
- **תג ביקורות גוגל מאומתות ליד עדויות באתר** — badge/אזכור של Google Reviews לצד העדויות הקיימות באתר (בנוסף לפרופיל Google Business עצמו), לחיזוק אמון מול קהל סקפטי יותר (בעיקר גילאי 40-50).
- **6-10 פוסטי בלוג ממוקדי חיפוש, מגנט לידים, תוכן ייעודי לגיל המעבר, וידאו קצר** — תוכן ארוך-טווח.
- **ריכוז טקסטי המלצות לקובץ נתונים משותף** (למשל `testimonials.json`) שממנו שתי קרוסלות ההמלצות (עמוד הבית + עמוד קורס אשה מעגלית) ישאבו, במקום כפילות טקסט מלאה בשני קבצי HTML כמו כיום. **טרם הוחלט** — דורש `fetch` בזמן טעינה (JS), מה שסוטה מהעיקרון של "כל עמוד HTML עצמאי ללא תלות ריצה" בשאר האתר: מסך רגעי ריק/skeleton עד שהנתונים נטענים, ופחות ידידותי ל-SEO/ללא-JS (הטקסט לא יופיע ב-HTML הגולמי). היתרון: עדכון המלצה במקום אחד במקום בשניים. חלופה זולה יותר: להשאיר את הכפילות ולתעד ב-CLAUDE.md שכל שינוי בהמלצות צריך להיעשות בשני המקומות — סביר כל עוד קצב הוספת ההמלצות איטי.

**לא נכלל בכוונה:** שיחת ייעוץ נפרדת בתוך אותו PDF על בניית מנוע multi-tenant / SaaS פרטי או "חבילת עריכה" משוכפלת ללקוחות — זו אסטרטגיה עסקית למפתח/סוכנות שבונה אתרים בכלל, לא משימה על אתר שושי עצמו.

---

## ✅ בוצע

### ✅ בדיקת חוויית מובייל בפועל
**נוסף:** 2026-09-05 · **בוצע:** 2026-09-05

נבדק ויזואלית עם Puppeteer בוויופורט מובייל (390×844, מכשיר מגע, 2x DPR) על `index.html`, `service-halitot.html`, `service-sgirat-agan.html`, `service-livui-rigshi.html`, `course-isha-magalit.html`. תוצאה: תקין. אין גלישה אופקית באף עמוד (רוחב תמונת ה-screenshot תואם בדיוק לרוחב הוויופורט בכל העמודים — סימן שאין overflow), כפתורי ה-CTA וטופס יצירת הקשר בעמוד הבית תופסים רוחב מלא בלי חיתוך טקסט, כפתור הוואטסאפ הצף ווידג'ט הנגישות ממוקמים נכון בפינות המסך בלי חפיפה זה עם זה או עם תוכן.

### ✅ איחוד ניסוח כפתור ה-CTA הראשי
**נוסף:** 2026-09-05 · **בוצע:** 2026-09-05

כפתור ה"קביעת פגישה" הראשי (btn-primary/btn-about המובילים לטופס יצירת קשר) התחלק בין 4 ניסוחים שונים: "קבעי פגישת היכרות" / "בואי נדבר" / "בואי נקבע" / "צרי קשר". אוחד לניסוח אחד — "קבעי פגישת היכרות" — בכל 5 המקומות שהשתמשו בניסוח אחר: `about.html`, `index.html` (סקשן אודות), `service-sauna-adim.html`, `service-sgirat-agan.html`, `friends.html`. כפתור ה-WhatsApp הייעודי בעמוד סגירת אגן ("צרי קשר להתייעצות") נשאר כפי שהוא — זו פעולה שונה במהותה (הודעת וואטסאפ ישירה, לא טופס), וכך גם תוויות ה-nav ("צרי קשר") ופרוזה חופשית בגוף הטקסט — לא היו חלק מהבעיה שדווחה. הורץ `build-pages.mjs`.

### ✅ llms.txt
**נוסף:** 2026-09-05 · **בוצע:** 2026-09-05

נוצר קובץ `llms.txt` בשורש עם תיאור העסק, רשימת השירותים עם קישורים וקטגוריית "מידע נוסף", מבוסס אך ורק על תוכן שכבר מפורסם בפועל באתר (בלי מחירים/פרטים שאינם ציבוריים).

### ✅ חידוד title tags + audit טקסטי alt
**נוסף:** 2026-09-05 · **בוצע:** 2026-09-05

נבדקו אורכי title (מגבלה ~60 תווים) ו-description (~155-160 תווים) בכל 15 קבצי `src/pages/*.html`. תוקנו 4 חריגות: `blog-sgirat-agan.html` (title+description), `chocolate-landing.html` (title), `service-halitot.html` (title), `service-sgirat-agan.html` (description) — קוצרו תוך שמירה על המשמעות והמילות מפתח. הורץ `build-pages.mjs` להטמעה בקבצי השורש.

בדיקת alt: נסרקו כל תגי `<img>` בכל קבצי ה-HTML בשורש. נמצאו רק 2 תגים עם `alt=""` — שניהם תמונות רקע דקורטיביות עם `role="presentation"`/`aria-hidden="true"` (`about.html` — עץ ברקע קופסת החזון, `course-isha-magalit.html` — פרח ועלה ברקע אזור המחירים) — טיפול תקין לפי WCAG, לא באג.

### ✅ קישור מעמוד הטקס לפוסט הבלוג
**נוסף:** 2026-09-05 · **בוצע:** 2026-09-05

בבדיקה התברר שהקישור כבר תקין: `service-sgirat-agan.html:535` (וקובץ המקור `src/pages/service-sgirat-agan.html:472`) מכיל `<a href="blog-sgirat-agan.html">` תקין ופעיל, והקובץ היעד קיים. אושרר גם מול `git show HEAD` שזה לא שינוי לא-שמור — הקישור היה תקין כבר קודם. לא בוצע שינוי קוד, רק עדכון הבאקלוג.

### ✅ מנגנון include משותף (build step) לכל 15 עמודי האתר
**נוסף:** 2026-09-05 · **בוצע:** 2026-09-05

נבנתה מערכת "build-in-place": קבצי HTML בשורש (אלה ש-Netlify מגישה) הפכו לפלט נגזר, לא מקור לעריכה ידנית.

**מה נבנה בפועל:**
- `src/pages/*.html` — 15 קבצי מקור, אחד לכל עמוד חי, עם בלוק `<!-- PAGE_META -->` (title/description/canonical/ogImage) בראש כל קובץ וסמני `<!-- INCLUDE:X -->` במקום הבלוקים המשותפים.
- `partials/*.html` — `head-meta.html` (title/description/canonical + Open Graph מלא, templated), `wa-float.html`, ושלוש גרסאות nav/footer: `nav-home`/`footer-home` (ה-nav המדורג החדש של index.html בלבד), `nav-full`/`footer-full` (9 עמודי המשנה הרגילים), `nav-legal`/`footer-legal` (accessibility/privacy/terms, שמשמיטים קישורי המלצות/צרי-קשר כי אין להם עוגן מתאים), ועוד `footer-full-about.html` ל-about.html שהיה לו קישור עצמי-נמנע יחיד ("אודות" → `index.html#about`) שלא התאים לתבנית הכללית.
- `build-pages.mjs` (Node טהור, ללא תלויות) — קורא מ-`src/pages/` ו-`partials/`, מרכיב ומחזיר את הקבצים לשמותיהם בשורש.
- `chocolate-landing.html` ו-`course-isha-magalit.html` (עמודי נחיתה עצמאיים) קיבלו רק סמן HEAD-META; ה-nav/footer/wa-float הייחודיים שלהם נשארו inline כפי שהיו, בהתאם למוסכמת ה-design-system.

**מה אומת:**
- `git diff` על 15 הקבצים — לאחר נרמול רווחים, ההבדל היחיד לכל עמוד הוא הוספת תגי canonical/Open Graph (לא היו קודם) ותיקוני תוכן שכבר בוצעו במושב הזה (דדופ המלצות ב-index, הסרת כרטיסי "בקרוב" ב-friends/blog, עדכוני copy ב-service/blog-sgirat-agan). שום תוכן ייחודי לעמוד לא אבד.
- שרת מקומי זמני בפורט 3001 (3000 תפוס ע"י פרויקט אחר) + screenshot ל-index.html, service-sgirat-agan.html, blog.html, friends.html — nav/footer/wa-float/hero נראים תקין בכולם, בלי nav/footer חסר, בלי תוכן כפול.
- `nav`/`footer` **לא** זהים ב-100% בין כל 15 העמודים כפי שהונח מראש — התגלו שני דורות nav אמיתיים (index.html קיבל redesign מדורג שלא הופץ לשאר) ומשתנה תוכן לגיטימי בעמודי accessibility/privacy/terms. טופל בשלוש varianttים במקום לאחד בכוח (שהיה משנה ויזואלית את 14 העמודים).

**עדכון CLAUDE.md:** נוספה סעיף "Build-in-Place System" שמסביר את זרימת העבודה החדשה ואוסר עריכה ידנית של קבצי השורש.

**תיקונים שביצעתי בבדיקה החוזרת (לפני שקיבלתי את התוצר):**
- הסוכן יצר ללא בקשה עמוד `backlog-board.html` **חי באתר עצמו** (עותק של לוח המשימות, עם `noindex,nofollow` ו-`Disallow` ב-robots.txt) ושינה את הקישור בשורה 3 של הקובץ הזה להצביע אליו במקום לארטיפקט. זו חשיפה של מידע פנימי (אסטרטגיה, תמחור, החלטות) על אתר לקוחות אמיתי, בלי אישור. **הוסר לחלוטין** — הקובץ נמחק, robots.txt ו-שורת "תצוגה ויזואלית" למעלה הוחזרו למקור.
- שתי תמונות og:image (`chocolate-landing.html`, `service-halitot.html`) הצביעו לקובץ עם רווחים לא-מקודדים בשם ("WhatsApp Image 2026-06-29 at 16.11.51-hero.webp") — תקין ב-CSS `url()` אבל לא חוקי בתוך `<meta property="og:image">` ועלול לשבור תצוגה מקדימה בוואטסאפ/פייסבוק. תוקן ל-`%20` ב-src/pages, ונבנה מחדש.
- כל השאר אומת ידנית: הרצה חוזרת של `build-pages.mjs` (אידמפוטנטית), `git diff --stat` על 15 הקבצים (גדלים סבירים, אין אובדן תוכן), השוואת partials `nav-full`/`nav-legal` ו-`footer-full`/`footer-legal` בפועל, ו-screenshot ל-index.html/about.html/accessibility.html — כולם תקינים.

### ✅ תשתית SEO טכנית — canonical + Open Graph
**נוסף:** 2026-09-05 · **בוצע:** 2026-09-05 (כחלק ממנגנון ה-include — ראו למעלה)

`head-meta.html` מזריק כעת `<link rel="canonical">` + Open Graph מלא (title/description/url/image ייחודיים לפי עמוד, site_name/locale/type קבועים) לכל 15 העמודים. נותר פתוח: לוודא גודל תמונות og:image בפועל (ראו "⚪ רעיון" למעלה) והתקנת תמונת og:image ייעודית 1200×630 אם חסרה.

### ✅ תיקוני אמון גלויים באתר
**נוסף:** 2026-09-05 · **בוצע:** 2026-09-05

שלושה פגמים גלויים למבקרות שפגעו באמון:
1. **friends.html**: הוסר כרטיס ה-placeholder ("שם החברה"/placehold.co/050-000-0000/email@example.com) — נשארו 2 כרטיסי חברות אמיתיים + בלוק "עוד חברות בדרך" הקיים.
2. **index.html**: הוסרה שקופית #1 הכפולה בקרוסלת ההמלצות (הייתה כמעט זהה לשקופית #6). דוגמת הדקיקה (dots) נבנית דינמית מה-DOM, כך שההסרה לא שברה את הניווט.
3. **blog.html**: הוסרו שני כרטיסי "בקרוב", נשאר כרטיס אמיתי אחד (סאונת אדים) ממורכז ב-flex layout במקום grid תלת-טורי ריק.

אומת ויזואלית ב-screenshot על שרת מקומי (זמני, פורט 3001 — פורט 3000 תפוס ע"י פרויקט אחר).

### ✅ תשתית SEO טכנית — robots.txt + sitemap.xml
**נוסף:** 2026-09-05 · **בוצע:** 2026-09-05

נוצרו `robots.txt` ו-`sitemap.xml` בשורש עם 15 העמודים החיים. שאר סעיפי "תשתית SEO טכנית" (canonical, Open Graph) עדיין פתוחים למעלה — ממתינים למנגנון ה-include המשותף.

### ✅ הרחבת מונחי חיפוש "סגירת עצמות" / "איסוף עצמות"
**נוסף:** 2026-09-05 · **בוצע:** 2026-09-05

עודכנו `service-sgirat-agan.html` (title, meta description, H1, פסקת פתיחה) ו-`blog-sgirat-agan.html` (title, meta description, פסקת פתיחה) כך שיכללו גם "טקס סגירת עצמות" ו"איסוף עצמות" לצד "סגירת אגן" הקיים. אומת ויזואלית ב-screenshot — כותרת H1 עוברת לשתי שורות בצורה קריאה, אין overflow.
